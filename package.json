{
  "name": "deskop",
  "private": true,
  "version": "0.1.0",
  "workspaces": [
    "server",
    "app"
  ],
  "scripts": {
    "server": "npm run dev --workspace server",
    "app": "npm run dev --workspace app"
  }
}
